Cambridge COVID-19 Sounds 正式实验：CSD3 一键运行说明
====================================================

暂停通知（2026-09-03）
----------------------
请暂时不要直接sbatch run.sh。身份审计发现旧数据门把18个Web submission合并为一个
form-app-users participant；旧975人gate与config已经作废。更新代码后须先用当前
run_reconstructed_match_first_v2.sh在新输出目录重跑真实WAV数据门，并把新的public aggregate
结果返回复核。run.sh现已带版本guard，会在下载模型或训练前拒绝旧gate。

请先执行修正数据门（不加载模型、不需要GPU）：

cd /home/yl809/projects/covid
git pull --ff-only origin main

# 先定位已下载的官方Task-1音频子集；把下一行替换成真实目录。
find /home/yl809/rds/hpc-work/datasets -maxdepth 4 -type d -iname '*task1*' -print
TASK1_ROOT=/REPLACE/WITH/REAL/TASK1/AUDIO/ROOT
test -d "$TASK1_ROOT" || { echo "Task-1目录不存在"; exit 2; }

cd /home/yl809/projects/covid/external_validation/cambridge_covid_sounds
bash run_reconstructed_match_first_v2.sh \
  /home/yl809/rds/hpc-work/datasets/covid19/metadata \
  /home/yl809/rds/hpc-work/datasets/covid19 \
  /home/yl809/projects/covid/external_validation/cambridge_covid_sounds/cambridge_audit_output_webfix_v2 \
  "$TASK1_ROOT"

完成后先返回：

/home/yl809/projects/covid/external_validation/cambridge_covid_sounds/cambridge_audit_output_webfix_v2/public/

这一步会额外生成public/overlap_report.json：包含Task-2 UID/submission/平台计数、每UID录音数、
Task-1∩Task-2 UID与sample计数、train/validation/matched交集、跨split音频hash和纵向标签冲突。
Task-1只做inventory，不进入训练。项目负责人明确复核新gate为GO且overlap report通过之前，
不要执行后面的sbatch run.sh。

旧数据门曾通过但现已作废。正式实验所需的路径、Slurm资源、env_chao、模型下载与校验、
配置填充、训练和结果打包仍在run.sh中；修正数据门未复核前，版本guard会阻止训练。

一、首次更新代码
--------------

因为之前目录里已有一个只跑数据门的旧 run.sh，请先将它改名，再拉取新版：

cd /home/yl809/projects/covid/external_validation/cambridge_covid_sounds
mv run.sh run_gate_old.sh

cd /home/yl809/projects/covid
git pull --ff-only origin main
git rev-parse --short HEAD

更新后 commit 应不早于：40399c0

二、直接提交正式实验
------------------

cd /home/yl809/projects/covid/external_validation/cambridge_covid_sounds
sbatch run.sh

不需要手工再次运行数据门，不需要手工编辑 config.local.json，也不需要提前手工下载模型。

run.sh 已经固定：

- Git仓库：/home/yl809/projects/covid
- 实验目录：/home/yl809/projects/covid/external_validation/cambridge_covid_sounds
- 数据目录：/home/yl809/rds/hpc-work/datasets/covid19
- 修正数据门输出：/home/yl809/projects/covid/external_validation/cambridge_covid_sounds/cambridge_audit_output_webfix_v2
- 基础Conda环境：env_chao（只读取已验证的PyTorch版本和CUDA构建，不向其中安装新包）
- 独立正式环境：/home/yl809/rds/hpc-work/venvs/cambridge_formal
- 模型缓存：/home/yl809/rds/hpc-work/models/schema_audio_alignment
- Slurm account：MASCOLO-SL2-GPU
- partition：ampere
- 资源：1 GPU、8 CPU、64 GB、4小时（首次运行的保守上限）

脚本会自动完成：

1. 复核数据门仍为 GO；
2. 从env_chao读取已验证的torch版本和CUDA构建，创建不共享site-packages的独立RDS venv；
3. 在独立venv中安装相同版本/相同CUDA系列的torch和torchaudio；
4. 检查 torch、torchaudio 和 CUDA；
5. 下载或复用 AST、固定 revision 的 Phi-2、OPERA 和 OPERA-CT；
6. 校验 OPERA-CT checkpoint SHA-256；
7. 只填写 config.local.json 中的模型路径和正式解锁口令；
8. 断言 100 pairs、AST-6L、5 seeds、500 epochs 等冻结协议没有变化；
9. 运行 Phi-2 文本缓存；
10. 提取 AST-6L 和 OPERA-CT 表示；
11. 运行 Correct / Within-label / Global 三臂，5 seeds × 500 epochs；
12. 完成 retrieval、probe、COVID transfer 和 calibration 评测；
13. 打包只含聚合统计的 PUBLIC_RESULTS.zip。

三、查看任务和日志
----------------

查看队列：

squeue -u yl809

假设 sbatch 返回任务号 123456，查看正常输出：

tail -f /home/yl809/projects/covid/external_validation/cambridge_covid_sounds/cambridge_formal_123456.out

查看错误输出：

tail -f /home/yl809/projects/covid/external_validation/cambridge_covid_sounds/cambridge_formal_123456.err

第一次运行需要下载模型并安装缺少的依赖，所以启动阶段会比后续重跑慢。1小时不建议；
2小时可能够但偏紧；默认4小时兼顾排队和安全余量。文本缓存、表示提取和已经完整结束的
backbone可复用；如果在某个backbone的15个alignment runs中途超时，该backbone会从头重跑。
确认没有同一任务仍在运行后，才可重新执行sbatch run.sh。

如果 env_chao 中 torch 与 torchaudio 不匹配，脚本会在训练前停止并在 err/out 日志中明确
写出版本，不会带着错误环境继续训练。此时请把完整日志和两个版本发回，不要自行更换模型
或修改实验协议。

新版不会直接向env_chao安装package。env_chao只提供已验证版本/CUDA构建作为选择依据；
torch、torchaudio、pytorch-lightning、torchlibrosa等全部重新安装在独立正式环境中。独立
环境不使用--system-site-packages，不会读取或修改env_chao的site-packages。

四、任务完成后返回什么
--------------------

只需要返回：

/home/yl809/projects/covid/external_validation/cambridge_covid_sounds/cambridge_audit_output_webfix_v2/public/PUBLIC_RESULTS.zip

同时请告知：

- git commit SHA；
- Python、torch、torchaudio、transformers、CUDA 和 GPU 版本；
- Cambridge DTA 是否允许发表聚合结果表。

请不要发送或提交：

- cambridge_audit_output_webfix_v2/private；
- 原始音频；
- 逐行 metadata；
- participant manifest；
- matched pairs；
- embedding、checkpoint 或逐人 prediction。

五、关于 csd3: 前缀
-----------------

在 CSD3 登录节点或 Slurm 作业内部，文件路径必须写成：

/home/yl809/...

只有从个人电脑运行 ssh/scp/rsync 时才写成：

csd3:/home/yl809/...

例如项目负责人从个人电脑取回最终结果：

scp csd3:/home/yl809/projects/covid/external_validation/cambridge_covid_sounds/cambridge_audit_output_webfix_v2/public/PUBLIC_RESULTS.zip .

不要把 csd3: 写进 config.local.json 或 run.sh 内部路径。
