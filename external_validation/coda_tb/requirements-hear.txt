# Isolated extraction environment only.  PyTorch alignment/evaluation uses the existing env.
# Google's current HeAR support repository pins TensorFlow ~=2.18.0. TensorFlow
# 2.15 can read the SavedModel signature but cannot execute its newer VHLO.
tensorflow==2.18.0
numpy>=1.26,<2.1
pandas>=2,<3
soundfile>=0.12,<1
librosa>=0.10,<1
